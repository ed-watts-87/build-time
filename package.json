{
  "name": "build-time",
  "description": "Pass a date, returns an object with easy to use values to constuct your own time strings",
  "main": "index.js",
  "scripts": {
    "test": "jest test",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ed-watts-87/build-time"
  },
  "author": "Ed Watts",
  "license": "ISC",
  "devDependencies": {
    "jest": "^20.0.4",
    "semantic-release": "^7.0.2"
  }
}
